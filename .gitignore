# 依赖（体积巨大，任何人克隆后运行 npm install 即可）
node_modules/

# Vite 构建产物（部署时再 npm run build；不必提交）
dist/

# 本地环境变量（勿把密钥提交到 GitHub）
.env
.env.*
!.env.example

# 日志
*.log
npm-debug.log*

# 操作系统
.DS_Store
Thumbs.db
desktop.ini

# 编辑器（不需要同步个人 IDE 配置可以保留）
.idea/
*.swp
*.swo

# Vite 本地覆盖
*.local
